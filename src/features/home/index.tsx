import AppShell from '@/features/home/components/shell';

/**
 * Home Component
 * @description Main entry point for the home page, rendering the App Shell.
 *
 * components used:
 * - AppShell
 *
 * @returns {JSX.Element} The Home component.
 */

function Home() {
  return <AppShell />;
}

export default Home;
