import { Outlet } from 'react-router-dom';

/**
 * Protocols Component
 * @description Protocols component that serves as a wrapper for protocol-related routes.
 *
 * components used:
 * - Outlet
 *
 * @returns {JSX.Element} The Protocols component.
 * @example
 * <Protocols />
 */

function Protocols() {
  return <Outlet />;
}

export default Protocols;
