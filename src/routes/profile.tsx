import ProfileFeature from '@/features/profile';

/**
 * Profile Component
 * @description This component serves as the main entry point for the profile page of the application.
 *
 * components used:
 * - ProfileFeature: The main feature component for the profile page.
 *
 * @returns {JSX.Element} The Profile component.
 * @example
 * <Profile />
 */

function Profile() {
  return <ProfileFeature />;
}

export default Profile;
