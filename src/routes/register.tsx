import RegisterFeature from '@/features/register';

/**
 * Register Component
 * @description This component serves as the main entry point for the register page of the application.
 *
 * components used:
 * - RegisterFeature: The main feature component for the register page.
 *
 * @returns {JSX.Element} The Register component.
 * @example
 * <Register />
 */

function Register() {
  return <RegisterFeature />;
}

export default Register;
