import ProtocolsFeature from '@/features/protocols';

/**
 * Protocols Component
 * @description This component serves as the main entry point for the protocols page of the application.
 *
 * components used:
 * - ProtocolsFeature: The main feature component for the protocols page.
 *
 * @returns {JSX.Element} The Protocols component.
 * @example
 * <Protocols />
 */

function Protocols() {
  return <ProtocolsFeature />;
}

export default Protocols;
